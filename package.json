{
  "name": "url-shortener",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "gen": "wrangler types ./types.gen.d.ts",
    "fmt": "prettier --write ./src ./*.js ./public/*.html",
    "test": "vitest --run --logHeapUsage --dir ./src",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "vitest": "^3.1.1",
    "wrangler": "^4.6.0"
  },
  "engines": {
    "node": "20"
  },
  "volta": {
    "node": "20"
  }
}
