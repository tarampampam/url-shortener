{
  "name": "url-shortener",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "gen": "wrangler types ./src/server/types.gen.d.ts",
    "fmt": "eslint ./src --fix && prettier --write ./src ./*.{js,ts} ./static",
    "test": "vitest --run --logHeapUsage --dir ./src",
    "lint": "conc -n ts,es 'tsc -b --noEmit' 'eslint ./src'",
    "build:server": "vite build --mode server",
    "build:client": "vite build --mode client",
    "build": "conc -n server,client 'npm run build:server' 'npm run build:client'",
    "watch:server": "npm run build:server -- --watch",
    "watch:client": "npm run build:client -- --watch",
    "watch:wrnglr": "wrangler -c ./dist/wrangler.json dev --no-bundle --live-reload --show-interactive-dev-session=false",
    "dev": "conc -k --restart-tries 3 -n server,client,wrnglr 'npm run watch:server' 'npm run watch:client' 'npm run watch:wrnglr'"
  },
  "devDependencies": {
    "@eslint/js": "^9.23.0",
    "@types/node": "^22.13.13",
    "concurrently": "^9.1.2",
    "eslint": "^9.23.0",
    "globals": "^16.0.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.28.0",
    "vite": "^6.2.3",
    "vite-plugin-html": "^3.2.2",
    "vitest": "^3.0.9",
    "wrangler": "^4.4.1"
  },
  "engines": {
    "node": "20"
  },
  "volta": {
    "node": "20"
  }
}
